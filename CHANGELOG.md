# CHANGELOG

ABLESTACK Cockpit UI의 변경 이력은 이 파일에 기록합니다. RPM 버전은
`VERSION` 파일을 기준으로 관리합니다.

## Unreleased

### Changed

- RPM 버전 기준 파일을 API 저장소와 동일한 루트 `VERSION`으로 통일했습니다. 로컬 RPM 빌드는 이 값을 자동 반영하고, GitHub 태그·수동 릴리스 버전이 `VERSION`과 다르면 빌드를 중단해 RPM, Git 태그와 문서 버전의 불일치를 방지합니다.
- 현재 ABLESTACK 저장소에서 사용하지 않고 Cockpit Files 템플릿 설정과 외부 Weblate 저장소에 의존하던 `weblate-sync-pot`, `weblate-sync-po`, `cockpit-lib-update`, `tasks-container-update` GitHub Actions 워크플로를 제거했습니다. 태그 기반 RPM 릴리스 워크플로는 그대로 유지합니다.
- RPM 빌더 도움말의 이전 `noarch` 안내를 실제 생성 아키텍처인 `x86_64`로 수정했습니다.
- 올인원 구성에서 로컬 라이선스를 Ablecube 전체에 먼저 배포하고, VM/HCI Filesystem GFS 구성에 필요한 호스트별 IPMI IP와 공통 계정을 입력받아 STONITH까지 실행하도록 연결했습니다. HCI Filesystem은 RBD 생성 결과를 GFS 장치로 이어서 구성하며, CCVM bootstrap 뒤에는 실제 Wall 모니터링 최초 구성과 서비스 검증을 실행합니다.
- GFS 리소스 카드와 HCI 스토리지 클러스터 카드의 호스트 제거를 제품별 비동기 Job API에 연결했습니다. 화면은 HCI의 Ceph/SCVM 제거와 VM의 PCS/GFS 제거를 포함한 현재 단계를 2초 간격으로 조회해 표시하고, 실패한 단계의 API 오류를 진행 모달에서 바로 확인할 수 있습니다. 제거 확인창에는 해당 호스트의 가상머신이 다른 정상 호스트로 마이그레이션된 후 호스트가 삭제된다는 경고를 명시하고, 사용자가 영향 범위를 확인했다는 체크박스를 선택한 경우에만 `호스트 제거` 버튼을 활성화합니다.
- HCI 추가 호스트 구성에서 기존 `cluster.json` 호스트 행은 잠그고 신규 Ablecube/SCVM 관리·PN·CN 정보를 한 행으로 추가하도록 변경했습니다.
- 스토리지센터 가상머신 배포 화면을 실제 SCVM Job과 연결했습니다. 추가 호스트는 로컬 SCVM 생성 후 기존 Glue 클러스터 조인 단계를 별도로 표시하고 완료 결과를 확인합니다.
- 추가 SCVM은 API Health를 20초 간격으로 최대 5회 확인하고 성공 즉시 서비스 준비와 기존 Glue 클러스터 등록을 진행하도록 화면 단계 문구를 API 실행 흐름과 맞췄습니다.
- 클라우드센터 구성 화면의 CCVM bootstrap 대기 시간을 API의 Host/SCVM 노드 준비 및 CCVM 내부 bootstrap 최대 실행 시간에 맞춰 130분으로 조정했습니다.
- GFS 디스크 확장·삭제 표를 큰 모달의 한 줄 레이아웃으로 변경하고 긴 값은 가로 스크롤로 확인하도록 개선했습니다. GFS 마운트 정보는 멀티패스 환경에서 `mpath` 장치만, 싱글패스 환경에서 `sd*` 장치만 표시합니다.
- 마운트된 GFS 디스크를 선택하는 확장·삭제 화면에서 API의 `disk_id`가 여러 개여도 대표 UUID 하나만 표시하도록 수정했습니다.
- CLVM 디스크 추가 화면에서 별도의 `CLVM 디스크 구성 대상 장치` 제목 영역을 제거하고, GFS 디스크 추가 화면과 동일하게 안내문 아래에 디스크 이름·장치명·사이즈·유형·UUID 테이블을 바로 표시하도록 정리했습니다.
- 디스크 상세 정보가 GFS 마운트 상태와 무관하게 전체 디스크 인벤토리를 조회하도록 변경했습니다. 멀티패스 환경은 `multipath -ll`의 모든 map을, 싱글 패스 환경은 `lsblk`의 OS 영역 제외 디스크를 장치별 한 행으로 표시하고 대표 `/dev/disk/by-id/...` UUID 경로를 복사할 수 있습니다.
- CLVM 디스크 정보 모달의 한 줄 텍스트 목록을 삭제 화면과 동일한 테이블 형태로 변경해 순번, 디스크 이름, 장치명, 사이즈와 WWN을 열별로 구분해 표시합니다.
- 모든 상태 카드 실행 작업의 진행·완료·실패 표시를 공통 모달의 본문과 상태별 안내 스타일로 통일했습니다. 작업 결과는 카드 footer에 남기지 않으며, 성공하면 `완료` 버튼으로 모달을 닫고 실패하면 `닫기` 버튼으로 원인을 확인한 뒤 종료합니다. 외부 스토리지 동기화·재검색에서는 `성공 n/n` 내부 집계를 제거했으며, 클라우드센터 VM 시작·정지·마이그레이션·클린업·SSH Port 변경·모니터링 갱신도 같은 흐름으로 전환했습니다.
- 버튼 내부에 직접 배치했던 Spinner를 PatternFly 표준 로딩 버튼으로 통일해 모든 실행·확인·다운로드 버튼의 아이콘과 문구 간격을 일관되게 표시합니다.
- 설정파일 다운로드 화면에 생성된 보안 증적 ZIP을 `cluster.json`과 같은 목록 항목으로 추가하고, 최신 증적이 존재할 때 해당 화면에서도 다운로드할 수 있도록 연결했습니다.
- 보안 증적 생성 및 다운로드 버튼의 수동 Spinner를 PatternFly 기본 로딩 상태로 교체해 아이콘과 문구 사이 간격을 정돈하고, 실행 중 문구를 `증적 생성 중`과 `다운로드 준비 중`으로 명확하게 표시합니다.
- 보안 증적 화면에서 수집 대상, 점검 항목, SSH 포트, 제한 시간과 최대 출력 라인 입력을 제거하고 API 기본 수집 정책을 사용하도록 단순화했습니다. 제품별 자동 수집 대상과 PPTX·XLSX·TEXT 제공 형식을 밝은 안내 카드로 표시합니다.
- 취약점 조치 화면의 SSH PORT 변경은 `new_port`만 전달해 포트 변경 후 MOTD·crontab을 포함한 전체 보안 패치를 계속 실행하고, 클라우드센터 클러스터 상태 카드의 독립 SSH Port 변경 작업만 `port_change=true` 전용 모드로 실행하도록 분리했습니다.
- 취약점 조치가 성공하면 별도의 완료 알림을 대시보드에 표시하지 않고 배포 현황을 새로 조회해 완료된 취약점 조치 항목이 사라지도록 변경했습니다. 실행 모달에는 성공 상태와 `완료` 버튼을 표시하고 사용자가 확인 후 직접 닫도록 하며, 실패 시에도 원인 확인을 위해 모달을 유지합니다.
- 클라우드센터 VM 구성의 상태 확인 단계를 VM·Standalone에서는 `호스트 상태 체크`, HCI·HCI-Filesystem에서는 `호스트 및 SCVM 상태 체크`로 표시하도록 제품별 문구를 구분했습니다.
- 클라우드센터 VM 구성의 두 번째 실행 단계에 `클라우드센터 초기화`를 추가해 재구성 전에 기존 `cloudcenter_res`와 `/mnt/glue-gfs/ccvm*`를 정리하도록 연결했습니다.
- GFS 리소스 상태 카드의 펜스 장치, 잠금 장치, GFS2 마운트 장치 상태를 데스크톱 화면에서 3열 한 줄로 표시하도록 정렬하고, 좁은 화면에서는 세로로 전환되도록 반응형 레이아웃을 적용했습니다.
- GFS 리소스 상태 카드에 `glue-gfs_res` LVM 활성화와 `glue-gfs` Filesystem clone의 상태 및 노드별 상세를 표시하는 `GFS2 마운트 장치` 항목을 추가했습니다.
- API 기본 URL을 `http://127.0.0.1:18090`으로 변경하고, `/etc/ablestack/ablestack-api.env`의 `ABLESTACK_API_PORT`를 사용해 RPM에서 변경한 API 포트를 자동 반영하도록 변경했습니다. `cube.conf`의 `CUBE_API_BASE_URL`을 명시한 경우에는 기존 override가 우선합니다.
- Wall 모니터링 최초 구성 마법사가 입력한 CCVM·Cube·SCVM IP와 SMTP 설정을 `action=configure` API로 전달하도록 변경했습니다. API가 Netdive, Wall/Prometheus/Loki, 서비스 enable/start, SMTP, 서비스 상태와 전체 호스트 완료 상태를 모두 검증한 경우에만 마법사를 완료합니다.
- 클라우드 클러스터 카드의 모니터링 수집 정보 재갱신은 최초 구성과 분리해 `action=update`를 사용하고, 중지된 Wall 서비스도 함께 enable/start한 뒤 상태를 검증하도록 변경했습니다.
- 모니터링 구성 마법사의 고정 진행 화면을 실제 API 흐름으로 교체했습니다. 제품별 Host/SCVM/CCVM Health Check, CCVM 모니터링 대상 갱신, 전체 호스트 `bootstrap.wall` 완료 상태 반영을 순서대로 실행하고 각 단계의 진행·완료·실패와 재시도를 표시합니다.
- ABLESTACK-Standalone 모니터링 구성은 SCVM 없이 CCVM과 첫 번째 Ablecube 자체만 대상으로 사용하고, 호스트 수를 1대로 고정하도록 제품별 입력 구성을 정리했습니다.
- 모니터링 구성 마법사가 `cluster.json`의 제품 타입과 Host 정보를 조회해 호스트 수, CCVM 관리 IP와 Ablecube 관리 IP를 자동 입력하고, HCI 계열에서는 SCVM 관리 IP 입력 영역과 값도 함께 적용하도록 변경했습니다.
- 클라우드센터 bootstrap 실행 후 대시보드에 별도 완료 문구를 표시하지 않고, 실행 중이던 구성 모달을 완료 상태와 닫기 버튼으로 전환하도록 개선했습니다. 오래 걸리는 Mold 데이터베이스·서비스·System VM 템플릿 구성 작업임을 실행 중 안내에 표시합니다.
- 클라우드센터 구성에서 호출하는 CCVM bootstrap이 실행 호스트의 QEMU Guest Agent `guest-exec`를 거치지 않고 CCVM API로 직접 전달되어 CCVM 내부에서 실행되도록 변경했습니다.
- 제품별 실제 구성 완료 시점에 따라 상태 카드 조회를 활성화하도록 조정했습니다. ABLESTACK-VM은 GFS 구성 후 클라우드 클러스터와 CCVM 실행 후 클라우드 VM을 조회하고, HCI 계열은 SCVM 실행·bootstrap 및 CCVM 실행 상태에 맞춰 스토리지 VM·스토리지 클러스터·클라우드 VM·클라우드 클러스터를 조회합니다.
- 클라우드 VM 배포와 클라우드센터 구성을 분리해, 실행 중인 CCVM의 `클라우드센터 구성` 작업에서는 배포 마법사를 다시 열지 않고 확인 후 `/cube/ccvm/bootstrap`만 실행하도록 변경했습니다.
- 배포 현황의 단일 `/cube/deploy/status` 응답에 포함된 카드별 polling 정책을 사용해, 선행 구성이 끝난 상태 카드만 개별 API 조회를 시작하도록 변경했습니다. CCVM 실행 또는 bootstrap 완료가 확인되면 다음 배포 상태 갱신에서 Cloud VM·Cloud Cluster 카드 폴링이 자동으로 활성화됩니다.
- 단독 클라우드 VM 구성의 마지막 단계에서 CCVM 시작 후 public health를 확인하고 라이선스만 등록하도록 변경했습니다. CCVM bootstrap 상세 설정은 별도의 클라우드센터 구성 화면에서 Mold bootstrap Job보다 먼저 실행합니다.
- 제품 흐름에서 계산한 현재 단계의 필수 작업을 API `available_actions` 누락 여부와 관계없이 `단계 작업`에 표시하고, `클라우드 VM 구성` 단계와 버튼이 동일한 배포 마법사를 열도록 연결했습니다.
- 클라우드센터 VM 마법사의 클러스터 민감도 입력창을 최대 세 자리 숫자와 증감 버튼에 맞는 고정 너비로 축소했습니다.
- 상태 카드의 클라우드센터VM 자원·서비스·스냅샷·DB 백업, 스토리지센터VM 수명주기·자원변경, CLVM/GFS 디스크 관리, GFS 호스트 제거, Cube 호스트 초기화와 전체 시스템 종료 기능을 실제 ABLESTACK API에 연결했습니다. 스냅샷·호스트·디스크 선택 목록은 하드코딩 값을 제거하고 API 조회 결과를 사용하며, 모든 작업에 진행·완료·실패 상태와 완료 후 상태 새로고침을 적용했습니다.
- 클라우드센터 클러스터 상태 카드의 VM 시작·정지, PCS 마이그레이션·클린업, SSH Port 변경, 모니터링 수집 정보 업데이트를 실제 ABLESTACK API와 연결하고, 실행 중·성공·실패 상태 및 즉시 상태 갱신을 표시하도록 변경했습니다.
- 클라우드센터 VM 배포 완료 후 `닫기`에서 취소 확인창이 표시되지 않도록 수정하고, 완료된 모든 마법사의 종료 동작도 같은 방식으로 정리했습니다.
- 클러스터 구성 준비 마법사의 상태 표시를 기준으로 모든 마법사의 `대기`·`진행 중`·`완료`·`실패` 배지, 글꼴, 글자색, 행 배경색과 테두리 색상을 공통 스타일로 통일했습니다.
- 클라우드센터 VM 마법사의 클러스터 민감도 숫자 입력창을 값 범위에 맞는 간결한 너비로 조정했습니다.
- 클러스터 구성 준비 마법사의 PCS 노드 IP 선택값을 내부 `hostIp`/`hostPnIp` 필드에 올바르게 매핑해 `cluster.json` 미리보기 화면이 중단되고 API 주소가 누락되던 오류를 수정했습니다.
- 올인원 제어의 클러스터·스토리지센터 VM·GFS·클라우드센터 VM 입력 문구와 선택 항목명을 각 개별 마법사의 용어를 기준으로 통일했습니다.
- 클러스터 구성 준비 마법사의 HCI 계열 Host 목록에 PCS 노드 IP 기준 선택을 추가하고, Ablecube 관리 IP 또는 HOST PN(HPN) 선택값을 `pcs_cluster_list` API 요청과 `cluster.json` 미리보기에 반영하도록 변경했습니다.
- GFS PCS 초기화에서 고정 클러스터 이름(`cloudcenter_cluster`)과 PCS 인증값을 자동 적용해 별도 사용자 입력 없이 실행합니다.
- GFS 구성 중 `modify_lvm_conf`, PCS 인증, 클러스터 setup/enable/status 등 API 세부 실행 결과를 단계 및 호스트별로 표시합니다.
- GFS 구성 흐름에 `create-gfs`를 연결해 PCS locking 준비 후 shared VG/LV, GFS2 파일시스템과 PCS clone 리소스를 생성합니다.
- GFS 구성의 마지막 단계에서 전체 호스트의 `systemProfile.bootstrap.gfs_configure`를 `true`로 반영하고 성공한 경우에만 완료 처리합니다.

## [v1.0.0] - 2026-08-05

### Added

- 배포 현황 화면에 스토리지센터, 스토리지 클러스터, 외부 스토리지 GFS, HCI Filesystem RBD/GFS, 로컬 스토리지, 클라우드센터 단계를 추가했습니다.
- 올인원 배포 화면에서 제품 유형에 따라 SCVM bootstrap, RBD 준비, GFS 구성, 로컬 스토리지 준비 단계를 선택하고 필요한 값을 입력할 수 있도록 추가했습니다.
- 클라우드 리소스 구성 화면과 보안 증적 생성·조회·다운로드 화면을 추가했습니다.
- GFS 관리, Mold bootstrap, 보안 증적 API 클라이언트를 추가했습니다.

### Changed

- 라이센스 관리를 올인원 제어와 분리해 라이센스 등록 완료 뒤에만 올인원 제어를 열도록 변경했습니다.
- 올인원 제어 모달을 화면의 약 90%로 확장하고 제품 흐름 영역을 축소해 현재 입력값을 더 넓게 확인할 수 있도록 개선했습니다.
- 올인원 흐름에서 라이센스 단계와 PCS 클러스터 IP 직접 입력을 제거하고, PCS 대상은 Host 목록의 IP 정보로 자동 구성하도록 변경했습니다.
- GFS·로컬·스토리지 VM 구성의 디스크 입력을 텍스트 입력 대신 선택형 목록으로 변경하고, HCI 스토리지 VM은 감지된 passthrough 디스크를 host별로 자동 선택하도록 개선했습니다.
- GFS 스토리지 구성에서 VG/LV와 mount point 입력을 제거하고, 클라우드 VM 구성에서 GFS mount point를 제거한 대신 CPU(8/16)와 메모리(16/32/64 GiB)를 선택하도록 변경했습니다.
- systemProfile 자동 반영 선택을 제거했습니다. 제품 타입, Host 목록, 클라우드센터 VM 정보 순서로 입력을 정리하고, VM 스토리지 네트워크 전용 선택 시 Host 목록에 스토리지 네트워크 IP 열이 나타나도록 개선했습니다.
- CCVM 서비스 네트워크 직접 지정은 선택 시에만 서비스 Bridge와 IP 입력을 표시하도록 정리했습니다.
- ABLESTACK-VM 제품 타입은 기본 호스트명을 `localhost` 대신 `ablecube1`으로 표시하도록 변경했습니다.
- ABLESTACK-HCI 및 HCI Filesystem은 순번과 `ablecube1`~`ablecube3`만 채운 3대의 Host를 기본 표시하고, IP 등 나머지 Host 값은 비워두도록 변경했습니다. SCVM도 CCVM과 동일하게 CPU(8/16 vCPU)와 Memory(16/32/64 GiB)를 선택하도록 정리했습니다.
- ABLESTACK-VM의 Host 수를 1대로 고정하지 않고 최소 1대부터 추가·삭제할 수 있도록 변경했습니다. HCI Host 목록에는 PCS 노드 주소 기준을 추가해 Ablecube IP(관리망) 또는 HOST PN(HPN)을 선택하도록 했습니다.
- 클러스터 구성의 클라우드센터 VM IP, 관리 CIDR, Gateway, DNS 기본값을 비우고, 서비스 네트워크 설정에는 서비스 IP, CIDR, Gateway 입력을 추가했습니다.
- 클러스터 구성 준비 마법사의 SSH Key 신규 생성 모드에서는 기존 `.dat` 파일 선택·초기화와 `id_rsa`/`id_rsa.pub` 미리보기 영역을 제거하고, 기존 파일 사용 모드에서만 표시하도록 변경했습니다.
- GFS 스토리지 구성 마법사의 외부 스토리지 동기화 실행 중 상태 배지 옆에 스피너를 표시하도록 개선했습니다.
- GFS 구성 실행 단계에서 실패·완료 메시지와 마지막 PCS 알림 단계 사이에 여백을 추가해 오류 내용을 더 읽기 쉽게 개선했습니다.
- GFS 스토리지 구성 완료 단계의 `닫기`는 구성 취소 확인 없이 정상적으로 마법사를 종료하도록 수정했습니다.
- GFS 구성 진행 화면에서 Host별 내부 API 작업 결과는 숨기고, 사용자가 확인할 단계별 구성 상태와 실패 메시지만 표시하도록 정리했습니다.
- 클라우드센터 VM 배포 마법사의 클러스터 구성 프로파일은 ABLESTACK-VM에서 순번·호스트명·호스트 IP만 표시하고, HCI 계열에서는 초기 클러스터 구성과 같은 SCVM 관리·호스트 PN·SCVM PN·SCVM CN IP 열을 표시하도록 변경했습니다.
- 클라우드센터 VM 배포 마법사에서 관리 및 서비스 네트워크 VLAN 입력과 관련 검증을 제거했습니다.
- 클라우드센터 VM의 CPU·Memory 제목은 간결하게 유지하면서, 선택 목록과 설정 확인에는 선택한 `vCore`·`GiB` 값을 표시하고 미선택 상태도 명확히 안내하도록 정리했습니다.
- 클라우드센터 VM 배포 마법사의 CPU와 Memory 기본 선택값을 각각 `8 vCore`, `16 GiB`로 설정했습니다.
- 클라우드센터 VM 배포는 Health Check → `cloudinit.iso` 생성 → `/cube/ccvm/create` CCVM 생성 → lifecycle `start` 순서로 개별 API를 실행합니다. ABLESTACK-HCI는 Host와 SCVM을 점검하고 RBD 방식으로, ABLESTACK-VM은 Host를 점검하고 GFS 방식으로 실행하도록 제품별 흐름을 분리했습니다.
- CCVM의 선택형 서비스 네트워크 설정 이름을 간결하게 정리하고, 화면에 표시되는 CloudStack 명칭을 Mold로 통일했습니다.
- 프론트엔드 중심 프로젝트의 중복 GitHub Actions 실행을 줄이기 위해 CodeQL 분석 워크플로를 일반 push와 PR에서는 실행하지 않고, RPM 릴리스와 같은 `v*` 태그에서만 실행하도록 변경했습니다.
- RPM 패키지 이름을 `ablestack-cockpit-plugin`으로 정하고, 태그 버전을 유지하는 `v1.0.0` 형식과 Enterprise Linux 배포 식별자(`.el9`)를 RPM 파일명에 포함하도록 변경했습니다.
- Cockpit 플러그인 binary RPM의 대상 아키텍처를 `noarch` 대신 `x86_64`로 지정해 source RPM과 x86_64 RPM을 함께 배포하도록 변경했습니다.
- x86_64 RPM 빌드에서 정적 JavaScript/CSS 자산에 대해 빈 debugsource 서브패키지가 생성되던 문제를 수정하기 위해 자동 debuginfo/debugsource 패키지 생성을 비활성화했습니다.
- Rocky Linux 컨테이너에서 마운트된 Git 작업 디렉터리를 safe directory로 등록해 태그 기반 RPM 빌드가 소유자 검증 오류 없이 실행되도록 변경했습니다.
- 배포 진행 상태와 완료 조건을 스토리지 및 클라우드센터 구성 단위로 더 자세히 표시하도록 개선했습니다.
- 클러스터 구성, 미리보기 다운로드, 보안 패치 API 응답 처리와 상태 화면의 레이아웃을 갱신했습니다.
- 클러스터 구성 및 GFS 스토리지 설정 마법사의 입력 흐름과 스타일을 정리했습니다.
